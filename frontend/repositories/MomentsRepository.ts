/** Moments lifecycle — shared across contexts via /api/v1/moments */
export const MomentsRepository = {
  // Domain-specific moment APIs are routed through Personal/Group/Business repositories.
  // Extend here when generic moment endpoints are wired on web.
};
