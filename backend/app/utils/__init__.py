"""Shared, framework-agnostic utility helpers.

Small pure helpers reused across domains (formatting, IDs, time, etc.) that do
not belong to any single domain and must not import FastAPI or SQLAlchemy
sessions.
"""
